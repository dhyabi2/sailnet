<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <PreferenceCategory android:title="Identity">
        <EditTextPreference
            android:key="nick"
            android:title="Nickname"
            android:summary="Shown instead of your wallet address and device addresses in every log, trace and screen. Stored only on this phone."
            android:defaultValue="" />
    </PreferenceCategory>

    <PreferenceCategory android:title="Circuit">
        <ListPreference
            android:key="hops"
            android:title="Hops"
            android:summary="%s"
            android:defaultValue="3"
            android:entries="@array/hops_labels"
            android:entryValues="@array/hops_values" />
        <EditTextPreference
            android:key="exit_cc"
            android:title="Exit country"
            android:summary="Two-letter code, e.g. DE or US. Empty = any. Applies to the next circuit."
            android:defaultValue="" />
        <SwitchPreferenceCompat
            android:key="stealth"
            android:title="Stealth mode"
            android:summary="Never contact a Nano node from this network once the wallet state is cached. Ledger calls go through the circuit."
            android:defaultValue="true" />
        <SwitchPreferenceCompat
            android:key="censored"
            android:title="Censored network"
            android:summary="Use only bridges as entry, never probe listed relays, never contact a Nano node directly. Needs a bridge line."
            android:defaultValue="false" />
        <SwitchPreferenceCompat
            android:key="auto_connect"
            android:title="Connect on open"
            android:summary="Start the tunnel when the app opens. For always-on, enable Sailnet in Android VPN settings."
            android:defaultValue="false" />
    </PreferenceCategory>

    <PreferenceCategory android:title="Relays">
        <SwitchPreferenceCompat
            android:key="use_builtin_bridges"
            android:title="Use built-in bridges"
            android:summary="The three founding relays, compiled into the app."
            android:defaultValue="true" />
        <EditTextPreference
            android:key="bridges"
            android:title="Extra bridge lines"
            android:summary="Unlisted relays handed to you privately, one sail-bridge line per row. Bridges are preferred as entry."
            android:defaultValue="" />
        <EditTextPreference
            android:key="dns_upstream"
            android:title="DNS resolver at the exit"
            android:summary="Where the exit sends your name lookups. Default 1.1.1.1:53."
            android:defaultValue="1.1.1.1:53" />
    </PreferenceCategory>

    <PreferenceCategory android:title="Payments">
        <EditTextPreference
            android:key="anchor"
            android:title="Prepaid per circuit (XNO)"
            android:summary="Sent to the entry relay when a circuit is opened. Default 0.0005, about 25 MB."
            android:defaultValue="0.0005" />
        <EditTextPreference
            android:key="max_rate"
            android:title="Maximum price (XNO per MB)"
            android:summary="Relays asking more are not used. Default 0.00005."
            android:defaultValue="0.00005" />
    </PreferenceCategory>
</PreferenceScreen>
