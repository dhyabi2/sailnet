<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <PreferenceCategory android:title="Identity">
        <EditTextPreference
            android:key="nick"
            android:title="Nickname"
            android:summary="Shown instead of your wallet address and device addresses in every log, trace and screen. Stored only on this phone."
            android:defaultValue="" />
    </PreferenceCategory>

    <PreferenceCategory android:title="Circuit">
        <MultiSelectListPreference
            android:key="exclude_cc"
            android:title="Never exit through"
            android:summary="Countries whose relays must not be your exit. Tap to choose from the relays currently known."
            android:entries="@array/empty"
            android:entryValues="@array/empty" />
    </PreferenceCategory>

    <PreferenceCategory android:title="Censored networks">

        <EditTextPreference
            android:key="bridges"
            android:title="Bridge lines"
            android:summary="Only if the built-in entries are blocked where you are: paste bridge lines handed to you privately, one per row."
            android:defaultValue="" />
    </PreferenceCategory>

    <PreferenceCategory android:title="Run a relay, ride it free">
        <ListPreference
            android:key="mode"
            android:title="Network"
            android:summary="%s"
            android:entries="@array/mode_names"
            android:entryValues="@array/mode_values"
            android:defaultValue="mine" />
        <SwitchPreferenceCompat
            android:key="direct_stealth"
            android:title="Keep the disguise in Direct"
            android:summary="Off: Direct is fast — cells go as they come, no padding. On: the link keeps the idle-browser rhythm (slower); use on a censored network."
            android:defaultValue="false" />
        <Preference
            android:key="add_relay"
            android:title="Add my relay"
            android:summary="On the relay run: sailnode pair — it prints a six-digit code. Pick the relay here and enter the code. Nothing else to copy." />
        <EditTextPreference
            android:key="mine"
            android:title="My relays"
            android:summary="Accounts of relays you run, one per row. Each must be started with --owner set to this phone's wallet address (or --payout, if that is the same wallet). One of them becomes your exit and asks you for nothing; you still enter through someone else's relay, so nobody watching your relay sees you arrive."
            android:defaultValue="" />
    </PreferenceCategory>

    <PreferenceCategory android:title="Wallet">
        <Preference
            android:key="wallet_backup"
            android:title="Back up wallet"
            android:summary="Show the seed that holds your balance. Write it down: uninstalling this app deletes it, and no server keeps a copy." />
        <Preference
            android:key="wallet_restore"
            android:title="Restore wallet"
            android:summary="Put back a wallet from a seed you saved. The wallet on this phone is kept, not deleted." />
    </PreferenceCategory>

    <PreferenceCategory android:title="Payments">
    </PreferenceCategory>
</PreferenceScreen>
